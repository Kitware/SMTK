################################################################################
# Build vxl
################################################################################
if (PARAVIEW_BUILD_QT_GUI)
  add_subdirectory(operators)
  #  add_subdirectory(vtk)
  #add_subdirectory(widgets)
endif()
