Attribute System
================

The attribute system now provides operations to create,
delete, and rename attributes.
These were previously performed by GUI code (and still are
in the Qt GUI extensions), but in preparation for Trame
support, these operations are being made available.
